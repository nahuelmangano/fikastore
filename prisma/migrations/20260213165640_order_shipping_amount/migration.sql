BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Order] ADD [shippingAmount] DECIMAL(18,2) NOT NULL CONSTRAINT [Order_shippingAmount_df] DEFAULT 0,
[shippingMethod] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
