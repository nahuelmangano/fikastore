BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[EPickShipment] ALTER COLUMN [qrImage] NVARCHAR(max) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
